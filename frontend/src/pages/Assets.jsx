import { Typography } from 'antd'

const { Title, Paragraph } = Typography

function Assets() {
  return (
    <div>
      <Title level={3}>Assets</Title>
      <Paragraph>
        The asset table, search, filters, and add/edit form will be built here
        in a later phase.
      </Paragraph>
    </div>
  )
}

export default Assets